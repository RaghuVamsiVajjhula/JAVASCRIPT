// History object is a part of window object that provides access to browsers history.
// It allows developers to manipulate the browsers history and
// Change the current URL without reloading the page.

// Most commonly used methods in history is: pushState() method.

//This method allows developers to add new entry in browsers history and
// change current url without any reloading of page.
history.pushState(null, null, "https://www.example.com");
// above is the syntax.


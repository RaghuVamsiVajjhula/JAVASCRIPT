// Write a progam using prompt function to take input of age as value from user
// and use alert to tell him if he can drive!

var age=prompt("Enter your age:");
if(age>=18)
{
    alert("You can drive");
}
else
{
    alert("You cannot drive");
}

// This will work on browser  not on vs code.
